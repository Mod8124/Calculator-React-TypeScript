import { Icalcu } from '../interfaces/Interfaces';

export const Default = () => {
    
  const DEFAULT:Icalcu = {
    prev: '0',
    next: '',
    operation: '',
    display: '',
  };

  return {
    DEFAULT
  };
};

